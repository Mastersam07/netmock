## Netmock
>> A DevTools extension that enables developers to record, replay, and modify HTTP network responses directly within Flutter DevTools, eliminating the need for external proxy tools or backend modifications.